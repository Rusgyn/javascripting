// * EXAMPLE

// function example (firstArg, secondArg) {  
//   return [firstArg, secondArg]
// }

// console.log(example('hello', 'world'))  //OUTPUT: (2) ['hello', 'world']

function math (one, two, three) {
  let product = two * three
    let sum = product + one
  return sum 
}

console.log(math(53,61,67))